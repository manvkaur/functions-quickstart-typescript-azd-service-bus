{
  "name": "functions-quickstart-typescript-azd-service-bus",
  "version": "1.0.0",
  "description": "TypeScript Azure Functions Service Bus queue trigger quickstart using Node.js v4 programming model",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.11.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}